{
  "apps": [{
    "name" : "idap backend",
    "script": "index.js",
    "instances": "max",
    "exec_mode": "cluster",
    "env": {
      "PORT": 3000,
      "NODE_ENV": "development"
    },
    "env_production": {
      "PORT": 3000,
      "NODE_ENV": "production"
    }
  }]
}

// pm2 start index.js -i 0 --name "idap backend"

// pm2 start server/index.js -i 0 --name "idap backend"
