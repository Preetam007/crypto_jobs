{
  "apps": [{
    "name" : "idap jobs",
    "script": "index.js",
    "instances": "1",
    "exec_mode": "cluster",
    "env": {
      "PORT": 4035,
      "NODE_ENV": "production"
    },
    "env_production": {
      "PORT": 4035,
      "NODE_ENV": "production"
    }
  }]
}

// pm2 start index.js -i 0 --name "idap backend"

// pm2 start server/index.js -i 0 --name "idap backend"
